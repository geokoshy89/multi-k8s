sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t geokoshy89/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true geokoshy89/react-test npm test